node_modules/
dist/
coverage/
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# Machine-specific editor/agent settings and credentials.
.claude/
.codex/
*.pem
*.key
*.p12
*.pfx
*.log

# Clean source exports (generated by pnpm export:source).
/release/

# Optional locally mounted MCP implementations; never part of Bridge source.
/local-mcps/
/mounted-mcps/

# Local bridge configuration is intentionally user-owned.
mcp-bridge.json
mcp-bridge.local.json
mcp-bridge.json.*

# Local manual media fixtures used for MCP end-to-end testing.
mediatest/

# Local Codex module/thread/task bindings managed by the Bridge.
.mcp-bridge-state/

# Interrupted MCP contract-test workspaces are disposable local fixtures.
.mcp-contract-*/

# Local Codex Desktop reverse-engineering/probe corpus; useful for maintenance,
# but version-specific and far too large/noisy for the project history.
tools/codex-desktop-probe/
